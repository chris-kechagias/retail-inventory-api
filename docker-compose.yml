version: '3.8'

services:
  web:
    build: .
    ports:
      - '5555:5555'
    environment:
      - DATABASE_URL=postgresql+psycopg2://postgres.eqalmqkyguuclatfoctz:g.MRS%2Bwj4Kr%2F9%2AU@aws-1-eu-west-1.pooler.supabase.com:5432/postgres
    depends_on:
      - db
    volumes:
      - .:/usr/src/app

  db:
    image: postgres:15-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=retail_inventory_db
    ports:
      - '5432:5432'

volumes:
  postgres_data:
